{
  "name": "@onelineflow/worker-extraction",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/main.js",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.717.0",
    "@onelineflow/ai": "workspace:*",
    "@onelineflow/core": "workspace:*",
    "@onelineflow/db": "workspace:*",
    "@onelineflow/observability": "workspace:*",
    "@onelineflow/queue": "workspace:*",
    "ioredis": "^5.4.2"
  }
}
